# Exact versions this was tested with. Pinned so an install cannot silently pull in a
# newer release that breaks the Pinecone Documents API or ships something malicious.
pinecone==10.0.0
httpx==0.28.1
pyyaml==6.0.3
flask==3.1.3
